define(function(require){
  'use strict';

  var common = require('common'),
      item = require('item');
  require('jquery.superslide');

  /*初始化幻灯*/
  $('.home-slider').slide({
    titCell: '.nav li',
    mainCell: '.item',
    effect: 'fold',
    autoPlay: true,
    delayTime: 700,
    interTime: 5000,
  });
  common.toggleCategory(1);
});